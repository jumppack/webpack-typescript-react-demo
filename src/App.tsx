import React from 'react';

const App = () => {
  return (
    <div className="app-container">
      <h1>Webpack + TypeScript + React</h1>
      <p>This is a sample application set up from scratch using Webpack and TypeScript.</p>
    </div>
  );
};

export default App;
